{
  "vendoredAt": "2026-09-09T04:56:26Z",
  "gateway": {
    "repo": "loxilb-inference-gateway",
    "path": "api/swagger.yml + api/swagger-extras.yml",
    "commit": "99013277afe3e6c7e9f9685e478a2e304f1f548b",
    "dirty": false,
    "note": "clean re-vendor via sync-specs.sh --only gateway; byte-identical to the merged main commit"
  },
  "oam": {
    "repo": "loxilb-oam",
    "path": "docs/swagger.json (swag init)",
    "commit": "535d6cc",
    "dirty": true,
    "note": "refreshed 2026-08-05 for the instance-registration hardening (409 conflict + 404 on the loxilbs routes); dirty=true because that change is not committed yet"
  },
  "loxilb": {
    "repo": "loxilb",
    "path": "api/swagger.yml",
    "commit": "2d747448",
    "dirty": true,
    "note": "added 2026-08-13 (backward-compat Phase 1) from the v0.9.8.5 tag checkout; dirty=true is tree-wide (.gitignore/loxilb-ebpf) — api/swagger.yml itself matched HEAD. gateway/oam entries deliberately untouched this round (gateway checkout mid-feature on feat/pii-presidio-integration)"
  },
  "metricManifest": {
    "repo": "loxilb-inference-gateway",
    "path": "deploy/monitoring/manifest/metric-manifest.json",
    "commit": "7f3520644ba3902d7e646c8ac34d18e8f1a70c74",
    "dirty": false,
    "vendoredAt": "2026-09-08T07:32:16Z",
    "note": "vendored verbatim (byte-for-byte upstream); the UI version envelope is generated into src/api/gen/metric-manifest.json by scripts/gen-metric-manifest.mjs (npm run gen:api)"
  }
}
