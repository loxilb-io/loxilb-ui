{
  "vendoredAt": "2026-09-08T03:13:31Z",
  "gateway": {
    "repo": "loxilb-inference-gateway",
    "path": "api/swagger.yml + api/swagger-extras.yml",
    "commit": "3546adc57c9570218e41e5def19cf26018716dcc",
    "dirty": true,
    "note": "clean re-vendor at the PR #129 merge commit (maintenance drain contract; upstreams the 401/403/503 management responses and the 200 OperationResult mutation bodies), copied from a detached checkout. dirty=true covers exactly three deleted lines: the upstream file carries a stray duplicated '204': mapping key (empty first occurrence) on three operations - GET /config/params, GET /config/bgp/neigh/all, and GET /config/bgp/policy/definedsets/{defineset_type}/{type_name} - introduced by upstream commit 4313de06; go-swagger tolerates the duplicate (swagger diff reports the pair in sync) but the strict YAML parser behind gen:api rejects it, so the vendored copy drops the empty duplicate key and keeps the surviving '204': description: OK value. Reported upstream; returns to dirty=false once the gateway spec removes the duplicates. All prior UI-only contract corrections retired after a handler-source reconciliation of every difference: the 62 stale 204 overlays and 5 409 overlays describe behavior the handlers do not emit, and the schema/constraint overlays (UserSummary, api_key pattern, key_id required, burst_pct bounds, RawError extras envelopes) either moved upstream, were unbacked by handler code, or are tracked as gateway spec issues instead of local patches."
  },
  "oam": {
    "repo": "loxilb-oam",
    "path": "docs/swagger.json (swag init)",
    "commit": "535d6cc",
    "dirty": true,
    "note": "refreshed 2026-08-05 for the instance-registration hardening (409 conflict + 404 on the loxilbs routes); dirty=true because that change is not committed yet"
  },
  "loxilb": {
    "repo": "loxilb",
    "path": "api/swagger.yml",
    "commit": "2d747448",
    "dirty": true,
    "note": "added 2026-08-13 (backward-compat Phase 1) from the v0.9.8.5 tag checkout; dirty=true is tree-wide (.gitignore/loxilb-ebpf) — api/swagger.yml itself matched HEAD. gateway/oam entries deliberately untouched this round (gateway checkout mid-feature on feat/pii-presidio-integration)"
  },
  "metricManifest": {
    "repo": "loxilb-inference-gateway",
    "path": "deploy/monitoring/manifest/metric-manifest.json",
    "commit": "7f3520644ba3902d7e646c8ac34d18e8f1a70c74",
    "dirty": false,
    "vendoredAt": "2026-09-08T07:32:16Z",
    "note": "vendored verbatim (byte-for-byte upstream); the UI version envelope is generated into src/api/gen/metric-manifest.json by scripts/gen-metric-manifest.mjs (npm run gen:api)"
  }
}
