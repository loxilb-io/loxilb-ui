{
  "vendoredAt": "2026-08-28T02:39:44Z",
  "gateway": {
    "repo": "loxilb-inference-gateway",
    "path": "api/swagger.yml + api/swagger-extras.yml",
    "commit": "4b0532aff5e6",
    "dirty": true,
    "note": "Gateway Swagger baseline at this immutable commit plus UI-required contract corrections validated against the live Gateway; dirty=true records that the vendored contract is not byte-identical to the upstream commit"
  },
  "oam": {
    "repo": "loxilb-oam",
    "path": "docs/swagger.json (swag init)",
    "commit": "535d6cc",
    "dirty": true,
    "note": "refreshed 2026-08-05 for the instance-registration hardening (409 conflict + 404 on the loxilbs routes); dirty=true because that change is not committed yet"
  },
  "loxilb": {
    "repo": "loxilb",
    "path": "api/swagger.yml",
    "commit": "2d747448",
    "dirty": true,
    "note": "added 2026-08-13 (backward-compat Phase 1) from the v0.9.8.5 tag checkout; dirty=true is tree-wide (.gitignore/loxilb-ebpf) \u2014 api/swagger.yml itself matched HEAD. gateway/oam entries deliberately untouched this round (gateway checkout mid-feature on feat/pii-presidio-integration)"
  }
}
