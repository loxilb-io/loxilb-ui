{
  "vendoredAt": "2026-09-04T22:57:35Z",
  "gateway": {
    "repo": "loxilb-inference-gateway",
    "path": "api/swagger.yml + api/swagger-extras.yml",
    "commit": "c66ac429fc115bc328651bf91036bea613762b27",
    "dirty": true,
    "note": "3-way merge (git merge-file) of the previous vendored contract onto origin/main c66ac429: upstream base 4b0532aff5e6 -> target c66ac429 (adds /config/ai/model-profiles*, kvexactstatus, KvExactStatusEntry/KvExactEnforcement/AiModelProfileRegistry/AiModelProfileEntry, kvModelProfile/kvExactApiMode) while preserving the UI-required contract corrections recorded since 4b0532aff5e6 (401/403/503 management responses, legacy-tuple 409 docs, UserSummary); the single conflict (api_key_auth description) resolved to the upstream fc769691 wording. dirty=true records that the vendored contract is not byte-identical to the upstream commit. swagger-extras.yml: upstream unchanged between base and target; vendored file (with UI corrections) kept as-is."
  },
  "oam": {
    "repo": "loxilb-oam",
    "path": "docs/swagger.json (swag init)",
    "commit": "535d6cc",
    "dirty": true,
    "note": "refreshed 2026-08-05 for the instance-registration hardening (409 conflict + 404 on the loxilbs routes); dirty=true because that change is not committed yet"
  },
  "loxilb": {
    "repo": "loxilb",
    "path": "api/swagger.yml",
    "commit": "2d747448",
    "dirty": true,
    "note": "added 2026-08-13 (backward-compat Phase 1) from the v0.9.8.5 tag checkout; dirty=true is tree-wide (.gitignore/loxilb-ebpf) \u2014 api/swagger.yml itself matched HEAD. gateway/oam entries deliberately untouched this round (gateway checkout mid-feature on feat/pii-presidio-integration)"
  }
}
